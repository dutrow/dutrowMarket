/**
 * 
 */
package dutrow.bidbot.bo;

import static org.junit.Assert.*;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.junit.Test;

/**
 * @author dutroda1
 *
 */
public class BidOrderTest {
	private static Log log = LogFactory.getLog(BidOrderTest.class);
	
	// due to the shortness of time and educational value, 
	// I decided not to test simple getters and setters
}
